{{ ansible_managed | comment }}

[Unit]
Description=golang prometheus libvirt exporter
After=network-online.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/usr/local/bin/{{ prometheus_libvirt_exporter__filename }}
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
